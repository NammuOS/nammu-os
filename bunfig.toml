[test]
# BentoPDF is an audited donor repository with its own dependency graph and test suite.
# Nammu's root quality gate must not discover the donor's tests as product tests.
pathIgnorePatterns = ["bentopdf/**"]
