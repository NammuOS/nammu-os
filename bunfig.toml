[test]
# Donor repositories have independent dependency graphs and test suites.
# Nammu's root quality gate must not discover their tests as product tests.
pathIgnorePatterns = ["bentopdf/**", "umbrel/**"]
